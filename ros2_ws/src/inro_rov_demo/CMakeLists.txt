cmake_minimum_required(VERSION 3.8)
project(inro_rov_demo)
find_package(ament_cmake REQUIRED)
if(NOT EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/models/bluerov2/model.sdf")
  message(FATAL_ERROR "Run python3 src/inro_rov_demo/scripts/prepare_model.py from ros2_ws first")
endif()
install(DIRECTORY launch config worlds models DESTINATION share/${PROJECT_NAME})
install(PROGRAMS scripts/controller.py DESTINATION lib/${PROJECT_NAME} RENAME controller)
install(PROGRAMS scripts/drive.py DESTINATION lib/${PROJECT_NAME} RENAME drive)
ament_package()
